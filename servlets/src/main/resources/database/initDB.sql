CREATE TABLE IF NOT EXISTS book (
                                          id         INT PRIMARY KEY,
                                          title VARCHAR(50)  NOT NULL,
                                          image  bytea
);